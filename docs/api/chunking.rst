Chunking
========

The :class:`~.ChunkSize` class represents the shape of a chunk for a chunked
array. It contains methods for manipulating these chunks and indices on a
chunked array.

.. autoclass:: ndindex.ChunkSize
   :members:
